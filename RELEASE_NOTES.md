# 更新日志

> 本文件记录 htmlGenius 所有已发布版本对**用户**可见的变化。
> README 仅保留最近三个版本，完整历史在此。

---

## v1.0.9（2026-09-09）

- **本地开发网站可以整站导出**：登录团队后，`localhost`、`127.0.0.1` 等 HTTP 本地开发站点也会显示“复制全站评论给 AI”，并按完整 origin 从后端汇总评论。
- **已有团队会话稳定恢复**：即使没有开启 Google 自动登录，重新打开侧栏也会恢复仍然有效的 PageTack 会话，不再错误隐藏团队功能。
- **无评论页面也保留整站入口**：当前页面没有评论时，仍可导出同一网站其他页面的全部未解决评论。

## v1.0.8（2026-09-09）

- **侧栏 Comment 不再误报未选文字**：即使网站拦截了选区变化事件，PageTack 也会优先读取页面上当前可见的真实选区，不再出现文字明明处于蓝色选中状态、却提示“Select text on the page first”的情况。
- **顶部选区的工具栏保持可见**：文字贴近视口顶部时，Comment 工具栏自动放到选区下方，不会跑出屏幕。

## v1.0.7（2026-09-03）

- **全新原创 PageTack 图标**：扩展与 Chrome 商店改用以网页、图钉和字母 P 为核心的原创品牌图形，不再沿用原项目图标。
- **侧边栏更简洁**：移除顶部 Home 图标，只保留反馈、主题、语言和账号入口。
- **商店身份指向当前版本**：扩展内的商店版标识更新为 PageTack 当前 Chrome Web Store 项目。

## v1.0.6（2026-09-03）

- **评论失联自动恢复**：扩展刚安装或更新、页面脚本暂时失联时，侧边栏会自动重新连接并重放当前操作，不再要求用户刷新网页。
- **点击 Comment 必定有反馈**：评论草稿消息加入接收确认、短时重试和去重；网页临时折叠选区时会使用最近的有效选区，不再静默吞掉点击。
- **账号视图不再遮住草稿**：用户在账号或团队视图中从网页发起评论时，侧边栏自动返回评论页并显示输入框。

## v1.0.5（2026-09-03）

- **连续评论不再静默失败**：保存按钮会等待当前评论真正写入后才清理草稿，可以连续提交多条评论；网络或会话失败时保留原文并显示明确错误，不再伪装为保存成功。

## v1.0.4（2026-09-03）

- **验证码可以稳定输入和提交**：修复 60 秒重发倒计时每秒重建整个表单的问题。现在倒计时只更新按钮文字，不会再清空验证码、抢走输入焦点或阻止提交。
- **登录后评论可以正常读写**：修正团队同步请求被标注网站的跨域策略拦截的问题。现在在 SEOZZR 等任意 HTTP/HTTPS 网页登录团队后，都能正常加载、新建和同步评论，服务端仍会强制验证用户会话。
- **邀请链接可以正常打开**：修正生产 API 域名下邀请链接多了一层路径而返回 404 的问题；已分享出去的旧链接也保留兼容，无需重新生成邀请码。
- **Chrome 商店版可连接生产服务**：服务端加入商店分配的 PageTack 扩展 ID，避免商店版在登录和团队同步请求中被 CORS 拦截。

## v1.0.3（2026-09-03）

- **全新扩展品牌名**：Chrome 中的展示名更新为「PageTack – Web Annotation」，与项目开发代号区分开；保留原有扩展身份和数据，不会重复安装成另一个扩展。
- **邀请码 + 邮箱免密码入团**：打开邀请链接后直接填写邮箱、输入一次 6 位验证码即可加入指定团队，不需要注册密码或先完成 Google 登录；验证后会在当前设备自动恢复会话。
- **整站评论一次交给 AI**：团队模式新增「复制全站评论给 AI」，从服务端读取当前团队、当前网站域名下所有未解决评论，按页面分组生成结构化 Prompt；严格隔离其他团队和相似域名，达到导出上限时会明确提示。

## v1.0.2（2026-08-27）

- **验证码可以重新发送**：邮箱注册后，60 秒冷却结束即可在验证码页重新发送；重复点击不会再并发创建多次请求或让你卡在注册流程。
- **登录问题更容易定位**：匿名使用统计新增登录失败的阶段、固定结果码和扩展版本号，用于判断问题发生在授权、验证码还是服务端响应。仍不包含邮箱、登录令牌、OAuth 原始错误、页面内容或账号信息。
- **官网更容易被发现**：官网补齐站点地图、中文产品主题信息与软件结构化数据，便于搜索引擎理解 htmlGenius 是网页批注、HTML 编辑与本机 AI Agent 协作工具。

## v1.0.1（2026-08-24）

- **匿名使用统计扩展**：为帮助我们看清「从打开侧栏、编辑、评论到交给 Agent 修改」的完整使用路径，匿名统计新增了协作与任务链路指标（回复评论、看到他人评论、查看修改计划、确认计划、应用候选、切换团队、复制邀请、自动登录恢复、编辑时长等）。统计仍然完全匿名：不含页面内容、评论内容、网址、邮箱或任何账号信息，仅记录事件类型与次数，也无法用于识别个人。可在官网隐私政策查看完整说明。
- 统计口径修正：发送任务不再把「先看修改计划」的预跑计入，任务漏斗数据更准确。

## v1.0.0（2026-08-21）

htmlGenius 迎来 1.0 里程碑。本版重点：团队成员显示名称可自定义、本地 HTML 文件体验全面稳定。

- **修改你的显示名称**：在团队面板的成员列表里点自己那一行的「修改名称」，保存后立即同步显示给团队成员。你自己设置的名字不会被 Google / 飞书账号的原始资料覆盖；名字属于账号，切换团队也保持不变。
- **本地文件评论更稳定**：修复扩展更新后，已打开页面上可能残留旧版组件、导致选中文字看不到评论工具栏的问题；现在更新扩展后无需刷新页面即可正常评论。
- **本地文件打开侧栏即可评论**：进一步修复本地 `file://` 页面上打开侧边栏后、立刻选中文字偶发无法弹出评论的问题（组件就绪竞态）。
- **本地文档彻底不参与团队同步**：即使你已登录团队，本地文件（file:// 等私有文档）的评论也只保存在你自己的浏览器里、绝不上传——这从此前的产品承诺升级为代码级保证；侧栏也不会再对本地文档误显示「实时同步」状态。
- **登录后仍可编辑历史本地评论**：修复登录团队账号后，之前在本机留下的本地评论被误判为「他人评论」而无法编辑、删除的问题。
- **发送按钮文案**：未探测完成前按钮统一显示「发送给 Codex」，不再出现含义模糊的「运行」。
- **设置说明更准确**：「逐条编辑预览」实际仅 Claude Code 支持（此前误写为 Codex 也支持；Codex 与 Copilot 一样会直接生成完整候选），并将模式名称对齐为界面里的「精确：只改圈选处」。
- 团队面板标题现在只显示团队自定义名称，不再带「你的团队 -」前缀。

## v0.9.17（2026-08-19）

- **稳定性修复**：修复升级后部分用户页面批注组件未注入、侧边栏一直等待的问题（content script 注册改为更可靠的安装即用方式）。
- **本地文件评论恢复可用**：修复在本地 `file://` HTML 页面上打开侧边栏后，评论草稿无法出现、以及侧边栏收不到运行消息的问题。
- **评论列表更清晰**：评论卡片现在显示作者头像首字母和名字；自己的评论会标记「你」。
- **发送菜单可正常展开**：修复「发送给 Agent」的下拉菜单被浏览器样式持续隐藏，导致 Connection Center 无法进入的问题。
- **账号更安全**：登录接口加入限流保护，密码被脚本反复尝试时会自动暂时拒绝；服务端补齐防伪造登录凭据的强校验（缺安全配置时拒绝服务而非降级运行）。
- **团队数据更安全**：团队所有权转让、解散、移除成员等敏感操作现在留有审计记录，出现争议可溯源。
- 评论等内容提交时服务端会剥离不可见控制字符；网页响应补齐防点击劫持/防嗅探等标准安全头。
- 新增 gitleaks 密钥扫描 CI，防止未来误提交密码/密钥；全历史核查确认无凭据泄露。

## v0.9.16（2026-08-15）

- **邮箱登录**：没有 Google 账号也能使用团队协作——用邮箱注册（验证码确认）+ 密码登录，输入邮箱后自动识别该登录还是注册。注册不再默认替你创建团队，而是由你选择加入已有团队或新建。
- **团队模式全面板**：点头像进入独立的团队面板，覆盖完整流程：加入/创建工作区、邀请成员（生成链接即复制）、成员管理（移除、转移所有权）、团队重命名、多团队切换。界面与文案整体打磨（整行悬停、返回导航、分割线对齐等）。
- **更省流量**：实时同步的连接只保持在当前可见的页签上，后台页签自动断开（切回时自动补齐期间的评论），开很多页签也不再各占一条长连接。
- 修多设备账号错配：登录凭据与团队身份改为按设备成组保存，两台设备登录不同账号时不再串用身份。
- **匿名使用统计**：为改进产品，扩展会收集完全匿名的功能使用事件（事件名 + 固定枚举参数 + 随机安装标识；不含页面网址、页面内容、评论内容与账号信息），发送到 htmlGenius 自有服务器，网络可达时同时用于 Google Analytics 可视化。详见隐私政策「匿名使用统计」一节。
- **GitHub Copilot 现在能正常生成本地候选了**：此前用 Copilot 生成候选会因本地连接组件（bridge）的鉴权与工具适配问题必然失败（报「无法访问 source.html」「需要登录」或会话直接失败）。本次彻底修复：bridge 会**自动从系统钥匙串读取 Copilot 登录凭据**（无需手动填 token）、并修正 Copilot 的输出方式，现在 Copilot 可与 Claude Code / Codex 一样直接生成本地 HTML 候选（bridge 1.0.11）。
- **运行诊断与一键上报**：失败或「无变更」的运行会自动留存（关掉侧栏也不丢），可在「复制诊断」里查看最近若干条并勾选上报**脱敏**诊断；也可在设置里开启自动上报，便于定位问题。
- **连接检查更直观**：检查连接时 Agent 状态点会转圈、显示「正在连接…」；检测到新版 bridge 时连接中心会钉住并提示升级。
- **「先预览确认」说明更清楚**：逐条编辑预览仅 Claude Code / Codex 支持；Copilot 不支持此项，会直接生成完整候选（设置项旁已标注）。
- 本地连接组件指针更新到 bridge 1.0.11（含上述 Copilot 鉴权/输出修复）。

## v0.9.15（2026-08-04）

- **修 GitHub Copilot 生成候选必败**：之前用 Copilot 生成候选版本时会报「读取 source.html 被拒绝」而必然失败——本地连接组件（bridge）的读工具白名单漏了 Copilot 实际使用的读取工具。已修复（bridge 1.0.2），重新安装本地连接组件即恢复。
- **修 Codex 未登录却显示「已连接」**：只装了 Codex App、尚未登录时，连接检查会误报 Codex 已就绪；现在会正确提示「需要登录」（bridge 1.0.2）。
- **修候选完成的提示语**：候选生成完成后，提示从容易误解的「修改计划已发送给 Claude Code」改为清晰的「候选版本已生成」；计时停止即表示完成，不再像中途卡住。
- 本地连接组件版本指针更新到 bridge 1.0.2（含上述 Copilot / Codex 修复）。

## v0.9.14（2026-08-02）

- **精确编辑扩展到 Codex**：「精准修补」的确定性编辑（Agent 只给改动点、扩展精确应用、严格限定圈选范围）现在 **Codex 也支持**，以只读沙箱运行，机制与 Claude Code 一致。Copilot 的适配已完成、但真机验证暂未做，**本版先不启用**（Copilot 的精准修补暂仍生成完整候选，验证通过后会开放）。
- **修「轮次耗尽」报错看不懂**：之前精确编辑偶发只显示「任务失败：claude exited 1: 」这样毫无信息的报错（Agent 校验用尽轮次时触发）；现在会给出明确可读的提示，并放宽了轮次上限、优化了提示词，减少触发。
- **精确编辑结果不再带绿框**：候选版本里被改的文字不再出现额外的绿色高亮框——改过的内容直接沿用原样式、与正文浑然一体，精确编辑的结果就是一份成品文档（不会偷偷加任何你没要求的样式）。
- **候选页不再「继承」旧高亮、源文件评论不再误丢**：候选版本改为独立文档，旧文档的评论高亮不会再跨标签带到新文件；在候选页清理「失效评论」也不会误删源文件里仍有效的评论。
- **Agent 下拉更好懂**：发送按钮旁的 Agent 下拉里，正在使用的 Agent 会标「使用中」并带呼吸状态点，已连接但没在用的会显示「切换」提示；悬停高亮只跟随鼠标，状态一目了然。

## v0.9.13（2026-07-31）

- **「精准修补」改为确定性编辑，改动严格限定在圈选范围**：以前精确修补是把整页交给 Agent 重写、只能靠提示词约束「别改别处」；现在 Agent 只给出要改的若干处（替换哪段文字、给哪个元素设什么样式），由扩展**精确应用**，从机制上保证不动你没圈选的内容。
- **应用前先给你过目**：默认会先弹一份「将要应用的精确修改」清单（每处标注改前→改后；定位不到 / 超出范围的会单独标出），你勾选确认后再生成候选；也可在「高级选项」里改成「直接应用，事后在候选页审阅」。
- **候选页高亮改动区域**：打开候选版本时，实际被修改的文字会高亮标出，方便快速核对。
- 目前确定性精确编辑仅支持「精准修补」模式 + 已连接的 **Claude Code**；其他 Agent 或「局部优化 / 重新生成」仍按原方式生成完整候选。若 Agent 没能给出可用的精确编辑，会自动回退为生成完整候选，不会卡住。

## v0.9.12（2026-07-30）

- **首次安装后切到旧标签有引导了**：Chrome 不会往「安装前已打开的页面」注入扩展脚本，所以装完 htmlGenius 切到旧标签会一直停在「正在连接」。现在会提示「刷新页面以启用 htmlGenius」并带一键刷新按钮，点一下刷新即可连上（连上后提示自动消失）。

## v0.9.11（2026-07-30）

- **本地连接组件指向更新到 1.0.1**：安装引导命令现在安装修复后的 bridge（路径穿越校验、解码器不再卡死、artifact 读取限定在工作区、供应链加固等）。新安装会直接拿到 1.0.1；**已装 1.0.0 的老用户不会自动升级**（本地组件不随扩展更新），需在 Connection Center 重跑一次安装命令才会升到 1.0.1。
- 扩展本身相对 0.9.10 无新功能变化（仅更新指向的 bridge 版本）。

## v0.9.10（2026-07-29）

- **安全加固（多数无感）**：依一轮代码审计修复多处问题——未保存草稿恢复提示不再暴露可伪造入口、会话票据签名强化、本地连接组件的路径穿越与供应链（改用锁定依赖安装）、未鉴权接口与敏感信息泄露面收口。
- **更稳定**：Service Worker 重启或运行中关闭标签后，任务不再永久卡在「进行中」；切换标签时运行状态不再串到别的标签；本机 Agent 桥收到坏数据时不再整体卡死。
- **会话失效会提示**：登录过期时实时同步自动停止，并提示「登录已失效，请重新登录」，不再静默死循环重试。
- **本地文件访问提示更准**：新标签页不再误显示「允许访问本地文件」引导。
- ⚠️ **更新后需重新启用一次**：为修正本地文件访问提示的误显示，本版新增了「读取浏览活动」权限。Chrome 会在更新后停用扩展，请到 `chrome://extensions` 手动重新启用一次。

## v0.9.9（2026-07-29）

- **修复连续修改字号只生效一次**：圈选文字后连点不同字号，现在每次都生效（此前只有第一次或重新圈选后才生效）。
- **草稿恢复提示移入侧边栏**：刷新页面后检测到未保存修改的「恢复 / 丢弃」提示，从页面横幅移到侧边栏「另存为」上方，页面不再残留扩展 UI。
- **输入法选字回车不再误保存评论**：选字状态的回车只确认候选，再按一次才保存。
- **侧边栏顶部改版**：去掉顶部品牌文案，新增「前往官网」入口，图标整体左对齐。
- **工具栏色卡跟随按钮**：点文字色 / 高亮色时色卡出现在该按钮下方，不再总从工具栏最左弹出；清除格式图标与侧边栏统一。

## v0.9.4（2026-07-25）

- **修复所有用户无法连接的关键 bug**：v0.9.3 的本地连接组件在发布包里遗漏了两个共享模块（`change-contract.js`、`plan-validate.js`），它们仍按仓库相对路径 `../extension/*` 加载，在受管安装目录里 `MODULE_NOT_FOUND` → host 启动即崩 → Connection Center 对**所有走标准流程的用户**都报「未安装 / BRIDGE_NOT_INSTALLED」。现以 vendor 子目录（CJS 飞地）把它们打进包内，host 可正常启动；新增「隔离布局加载」回归测试，杜绝此类「包内模块依赖包外仓库路径」的问题再次漏网。

## v0.9.3（2026-07-25）

- **修复 `npx` 安装命令**：上一版安装包有两个命令入口，导致用户复制的 `npx --yes @htmlgenius/bridge@… setup` 报「无法确定要运行的可执行文件」。现已移除多余的 host 入口（host 本就由启动器以绝对路径拉起，无需作为命令），`npx @htmlgenius/bridge` 现在可直接运行，「复制 Terminal 命令 / 让 Agent 帮我连接」的正规接入路径恢复可用。

## v0.9.2（2026-07-25）

- **本地连接组件更可信**：`@htmlgenius/bridge` 现可在 npm 页面一键溯到公开 GitHub 源码，附带包级说明与 MIT 协议；发布改用 CI 带 **provenance（来源证明）**，安装时的「陌生新包」风险提示显著降低。
- **连接中心默认收起**：Connection Center 现在默认折叠（无论是否已连接），标题仍显示状态，点一下即可展开，不再占用契约页空间。
- **置灰有说明**：在远程网页上「发送给 Agent」按钮置灰时，会提示「直接发送仅支持本地 HTML 文件，可改用复制 Prompt」，不再让人摸不着头脑。
- **编辑页精简**：移除编辑帮助栏位、artifact 信息行（文档 ID / 基线哈希）与「重新读取文件」按钮；暂时隐藏较难懂的「切换高级模式」入口（元素级编辑能力保留）。
- **文案优化**：选择修改范围页提示改为「不指导修改文章的评论，扔掉效果更好」。
- **安装门槛放宽**：本地连接组件现支持 Node 20.x 与 22+（此前要求 20.19+/22.12+）；仅 GitHub Copilot 仍需 20.19+/22.12+，不满足时 Copilot 单独显示「不兼容」，不影响 Codex / Claude Code。

## v0.9.1（2026-07-23）

- **Agent 支持更可靠**：新增 Agent 现在必须通过统一的「能力认证」——探测、生成候选、生成计划、安全边界逐项验证通过后才会出现在你的发送菜单里，减少「显示支持却用不了」的情况。
- **连接状态更清楚**：Connection Center 逐个 Agent 给出真实状态（已连接 / 未检测到 / 需登录 / 版本不兼容）与对应做法；「复制诊断」只给脱敏信息，不含路径与隐私。

## v0.9（2026-07-23）

- **本地连接，面向所有人**：不再需要进源码仓库跑脚本。未连接时契约页出现 **Connection Center**：点「**让 Agent 帮我连接**」复制一段严格限定的安装说明粘贴给你正在用的 Agent，或「复制 Terminal 命令」自己执行——一条命令即可装好本地连接组件。
- **连接状态一目了然**：区分「未安装 / 需修复 / 组件就绪但 Agent 未登录 / 已连接 N 个 Agent / 系统不支持」，逐项给真实状态与官方登录指引；「检查连接」只读重探。
- **安全修复**：仅在判定可自修时出现「安全修复」（二次确认），明确只重写 Chrome 注册文件，不安装 Agent、不改你的项目文件。
- **没连上也能用**：任何状态下都可「复制 Prompt」手动交给任意 AI。
- 本地连接组件以 `@htmlgenius/bridge` 发布到 npm；仅 macOS + Chrome + Node 20.19+/22.12+。

## v0.8.2（2026-07-23）

- **新增 GitHub Copilot**：发送菜单第三个 Agent。优先用你本机的 `copilot` 命令行；没装时自动用 Copilot SDK 自带运行时，菜单实时显示所用方式。
- **受控安全边界**：Copilot 运行在受限模式——只开放文件读写并逐个校验路径（候选只允许写候选文件、计划只允许写计划文件），shell / 网络 / 子任务全禁；每次发送都是一个新会话，**绝不读取或续发你在 Copilot / VS Code 里的已有会话**。
- **安装更宽松**：装本地连接组件不再要求本机有 Claude CLI——只用 Copilot 或 Codex 也能装。

## v0.8.1（2026-07-22）

- **Codex 接入（推荐优先）**：除 Claude Code 外，可把修改契约交给本机 Codex（ChatGPT.app 内置）。Codex 是常驻热服务，握手快、逐字流式输出，整页重做通常几分钟完成；Claude Code 走命令行冷启动，等待更久属正常。两者都只写候选、永不覆盖原文件，只用本机登录态、不存任何凭证。
- **修改范围三档直达**：「基于评论修改文档」默认全选评论，直达「选择修改范围」——精确修补 / 局部优化 / 全文重做三档；全文重做不再强制填 brief，评论即输入。
- **Mint 深色主题**：整个侧边栏与页面级刷新弹窗统一为 Mint 配色。
- **候选版本号**：每次生成写入文件名（`原名V1.1.html`、`V1.2.html`…按文档累计），状态栏显示「候选 V1.N 已生成」。
- **生成过程可感知 + 可中止**：发送后按钮变「终止任务」（可随时中止）；状态栏自动展开显示 Agent 逐字输出与文件 / 命令 / 思考事件 + 计时器，完成后自动收起。
- **完成即开 + 通知**：自动新开页签打开候选 + 系统通知；回原 tab 发送按钮自动恢复，便于改评论后再发下一版。
- **多 tab 独立**：每个浏览器 tab 一份契约 / 运行态，切 tab 自动跟随。

## Night Pack A · 候选闭环（2026-07-20）

- **Claude 产出候选 HTML**：发送给 Claude Code 后，本机 Agent 在受控工作区写 `candidate.html`（**只放行 Read/Glob/Grep/Write，仍禁 Bash/Edit/网络/MCP，无 shell**），host 校验后把候选复制为源文件同级的 `<原名>--htmlgenius-<runId>.candidate.html`；**源文件全程不被覆盖**。
- **source 快照 + 双哈希**：运行前把源复制为只读快照（0400）并校验哈希；运行后再读源哈希，运行期被外部改动即 `SOURCE_MUTATED_DURING_CANDIDATE`、候选不被采用、不显示成功。
- **候选形态校验**：候选必须存在、非 symlink、非空、不超上限、且是真正的 HTML（`<!doctype html>`/`<html>`），否则拒绝（阻止模型写出 Markdown / 纯文本）。
- **打开候选 + 评论重锚**：候选经 v0.6.2 受控 `new_artifact` 路径打开（background 逐字段比对 run/task/source 哈希后才放行），新页评论自动重定位。
- **最小只读成功态**：侧边栏显示「候选版本已生成 / 原文件未被修改 / 已重锚 X/Y 条，Z 条待检查 / provider·时间·源→候选文件名·源哈希已校验·候选 hash 前缀」，并提供「打开候选 / 返回原文件」。**这不是「接受修改」**——无 promote / 覆盖 / 自动接受按钮或路径。
- **持久证据**：刷新侧边栏后，最近一次候选的证据仍可显示；run 记录只存元数据（候选 URI / 候选 hash / manifest 路径），**不存 prompt / 评论 / 候选 HTML / Claude 回复**。
- 真实 Claude 运行的 smoke 测试标记为「待用户手动验证」（夜间不消耗本地额度）；fake CLI 自动化测试覆盖写候选 / 写 Markdown 拒绝 / source 突变拒绝 / argv 注入安全。

## v0.7.2（2026-07-20）

- **评论 → 编辑任务的选择流**：打开评论页默认仍是完整评论收件箱（先讨论）；底部新增次级入口「**整理评论，创建编辑任务**」。点击后先在侧边栏内进入「**挑选这次要处理的评论**」步骤：默认全选所有顶层且未失效的评论，用户可手动取消本次不交给 Agent 的；回复随被选 root 完整带出，不单独勾选；超过 20 条给出分批提示。**没有任何模型或规则替用户判断一条评论是不是修改建议。**
- **状态可回退**：挑选 → 契约表单之间可往返，返回时保留本轮勾选与已填内容；关闭或 Esc 则清空临时选择与表单，再次进入恢复默认全选。未选/已失效评论绝不进入 Prompt 或 JSON。
- **模式文案更易懂**：四种修改目标改为「只改圈选处 / 优化这一区块 / 先给我改法 / 重做整页」，仍是互斥的四选一（不偷改 schema）；字段改为「你想让页面变成什么样？」「有绝对不能动的内容吗？（可选）」，长解释收进可键盘聚焦的 `?` tooltip。
- **布局**：评论列表与选择列表超出首屏只纵向滚动，底部操作按钮常驻第一屏；窄 Side Panel 下长文本换行、tooltip/菜单向内展开，**不出现横向滚动条**。
- 三语（中/英/日）i18n 完整；既有评论创建/回复/编辑/删除/定位、Change Contract、Claude handoff 全部不回归。

## v0.7.1（2026-07-20）

- **一键交接 Claude Code**：修改任务 Composer 中「精准修补 / 局部优化 / 重新生成」三种模式出现「**发送给 Claude Code**」——本机已登录的 Claude Code CLI（先 `claude auth login`）一键收到完整修改契约，不用切换应用、不用复制粘贴。「结构重组」仍只复制规划任务。
- **会话续发**：可选「继续 HTML Genius 上次创建的 Claude task」；只续发插件自己创建的 Claude 任务，绝不读取、枚举或注入你其他的 Claude 会话。
- **本版为交接验收版**：Claude 只确认收到任务，**不修改任何文件**（源文件在交接前后做 SHA-256 双校验，有变化即失败并报因）。真正的受控编辑执行见后续版本。
- **本地传输证据**：完整契约落盘 `<源文件目录>/.htmlgenius-bridge/claude/<文档ID>/task-*.{json,md}`（0600 权限），可本地审计。
- **安装**：`cd bridge && node install-macos.mjs --extension-id <扩展ID>`，详见 `docs/LOCAL_BRIDGE.md`。仅 macOS + Node 20+；host 名改为 provider-neutral 的 `com.htmlgenius.local_bridge`（后续 Codex adapter 复用）。

## v0.8（2026-07-20）

- **高亮不再「盖字」**：页面工具栏与侧边栏的调色板统一为同一份取值，并移除会让浅色文字隐形的纯白高亮；对失效选区施色不再静默无反应，而是明确提示「请先在页面选中文字」。
- **重做真正可用**：此前「打字后马上点撤销」「工具栏改色/改字号」会导致改动丢失、redo 回不来；现在撤销/重做完全对称，工具栏的所有编辑（颜色 / 字号 / 标题 / 对齐 / B/I/U/S / 清除格式）都记入撤销历史。
- **侧边栏工具补齐**：新增评论 / B/I/U/S / 清除格式 / 字号 / 标题 / 对齐，与页面工具栏走**同一份修改逻辑**（唯一入口），B/I/U/S 点亮状态两边实时同步。
- **图标统一**：侧边栏文字色（A 带红底条）/ 高亮色（荧光笔黄笔头）图标与页面工具栏完全一致。
- **选色弹窗整齐**：改为整面板宽浮层，8 列 × 2 行色格无空位，不再超出面板右边界。
- **「生成修改任务」常驻第一屏**：评论再多，该按钮固定在底部；评论区独立滚动（底部操作区为可扩展容器）。
- **编辑文字立即出光标**：高级模式点「编辑文字」后立即出现闪烁光标（不用再点一下控件），选框同时闪一圈作反馈。
- **修复「刷新」按钮**：激活确认弹窗点「刷新」偶发无反应的问题已修复（即便页面拦截了刷新也会兜底进入编辑）。
- **评论文案统一**：「批注 / 评论」不再混用，全部统一为「评论」（中 / 英 / 日）。

## v0.7（2026-07-19）

- **本机 Codex Local Bridge（实验性）**：Native Messaging host 把修改契约交给你本机已登录的 Codex，产出一份新的 HTML candidate（**只写 candidate，不覆盖原文件**），打开后批注自动重定位。仅 macOS + Chrome + Node 20+。
- host 名采用 provider-neutral 的 `com.htmlgenius.local_bridge`（v0.7.1 起默认走 Claude Code，Codex 作为后续 adapter 复用同一 host）。

## v0.6.2（2026-07-18）

- **本地 HTML 版本对账**：引入逻辑文档 ID + artifact SHA-256；本地 HTML 被外部（如另一个 agent）改过后，打开插件看到的是**新版本**而非带旧评论的旧快照；版本关系可追溯，为后续受控写回提供 base hash 校验基础。

## v0.6.1（2026-07-18）

- **修改契约任务生成器**：把评论升级为带「允许范围 / 保护规则 / 歧义处理 / 验收条件」的结构化修改契约，支持精准修补 / 局部优化 / 结构重组 / 重新生成四种模式；可复制 Prompt 或 JSON。

## v0.6（2026-07-16）

- **元素级编辑（高级模式）**：在本地 HTML 上选中 / 删除 / 复制 / 拖拽重排控件，调整字体 / 字间距 / 行距 / 内边距。
- **样式与 Emoji 工具**：浮动工具栏与侧边栏补齐文字颜色 / 荧光笔 / 字号 / 标题 / 对齐 / Emoji。
- 多项编辑体验修复（选区高亮、工具栏与编辑态联动等）。

## v0.5.2（2026-07-15）

- **撤销 / 重做 / 还原**：编辑历史改为线性模型，撤销（Cmd/Ctrl+Z 或侧栏按钮）与重做真正可用；新增「还原到本次编辑初始」一键回基线。本地页与远程页（临时编辑）均生效。
- **侧边栏编辑工具栏**：编辑态新增撤销 / 重做 / 还原初始 / 另存为 HTML 四个会话动作；「另存为」把当前正文下载为 .html（本地/远程均可，无法覆盖原文件）。
- **原生色环取色**：侧边栏文字色 / 高亮色改用系统原生取色器（自由选色）；浮窗色板保持不变。
- **点击高亮聚焦评论**：在页面点已有批注的高亮，侧边栏自动切到批注 Tab、滚到该卡片并聚焦回复输入。
- **在线人数移至身份入口**：评论区不再显示「在线：姓名」，改在账号浮层显示「本页在线 N 人」；登出即清。
- **编辑模式 alert 气泡**：编辑视图顶部用标准 alert 组件（本地=绿「可保存」/ 远程=黄「临时」），浅色主题用标准暖黄/浅绿。
- **修复撤销失效**：此前撤销监听仅在 file:// 本地页注册、且首次撤销空操作；现已全局可用。

## v0.5.1（2026-07-14）

- **深色 / 浅色主题切换**：侧边栏右上角一键切换；深色 Nebula 风格，浅色参照 Airtable 设计规范（白底 + 近黑主按钮）。浮动工具栏与确认弹窗跟随切换；无偏好时跟随系统 `prefers-color-scheme`。
- **中 / 英 / 日 多语言**：跟随浏览器语言，可在头像旁切换并本地记住。
- **评论可编辑**：作者可编辑自己已保存的评论。团队模式经后端 `PATCH /api/annotations/:id`（作者校验，跨团队/非作者 403），编辑经 SSE 实时同步给队友。
- **收起侧边栏即停止**：关掉侧边栏后，浮动工具栏 / 高亮 / 编辑态立即消失（端口长连接断开触发），页面恢复正常浏览。
- **失效评论一键清理**：网页内容变动后找不到原文位置的旧评论，归档到侧边栏底部，可一键全部删除。
- **修复本地 HTML 版本陈旧**：若本地 HTML 被外部（如另一个 agent）改过，打开插件时展示**新版本**而非带旧评论的旧版本（基于正文哈希判定）。
- 编辑工具栏：加粗 / 斜体 / 下划线 / 删除线支持切换高亮与共存；修复字号、对齐不生效；文字颜色、荧光笔换标准 SVG 图标。
- 后端 CORS 放开 `PATCH` 方法。

## v0.5.0（2026-07-07）

- **多人协同开放**：飞书 OAuth 登录后，团队可实时共享同一页面的批注与回复。
- **身份硬化**：批注作者 = 飞书账号（open_id），后端校验；仅作者可删自己的批注（此前 team-token 下可伪造）。
- **登录即用**：侧边栏填后端地址 → 「飞书登录」；本地模式不受影响。`HG_TEAMS` 下线，改用 session token。

## v0.4.2（2026-07-06）

- **修复**：本地 HTML 的编辑现在能保存——此前编辑虽写入本地存储但从不还原，关闭/刷新后丢失；现启动时自动恢复上次编辑。
- **修复**：Ctrl+Z 撤销不再让浮工具栏失灵——此前撤销用整段 body 替换，会丢掉工具栏的事件监听；现保留同一工具栏节点。

## v0.4.1（2026-07-06）

- **界面换新**：侧边栏与浮工具栏采用更清爽的暖色风格。
- **不再弹浏览器对话框**：批注的创建、回复、删除全部改在侧边栏里直接完成；Enter 保存、Shift+Enter 换行、Esc 取消。
- **自带字体**：无需联网加载字体。

## v0.4（2026-07-05）· 多人协同

- 多人可在同一页面**实时**看到彼此的批注与回复。
- 批注支持**层层回复**，形成讨论线索。
- **只能删除自己的批注**，删除会连带其下所有回复。
- 显示当前**在线的队友**。

> 协同入口暂时隐藏，等接入飞书登录后开放；当前本地单人使用完全不受影响。

## v0.3（2026-06-30）· 升级为 Chrome 扩展

- 从网页版升级为 **Chrome 扩展**：装一次，就能在任意网页上批注，不再限于固定样本。
- **自动区分**：本地文档（`file:` / `localhost` 等）默认可直接编辑；远程网页默认只读批注，手动开启编辑时会提示这是临时修改。

## v0.2（2026-06-27）· 可直接编辑 HTML

- 可以**直接在渲染好的 HTML 上改文字、调样式**（加粗 / 颜色 / 字号 / 对齐）。
- 选中文字即出现**浮动工具栏**。
- 编辑**自动存版本**，可回退；`Ctrl+Shift+Z` 撤销。

## v0.1（Beta，2026-06-25）· 首个版本

- **划词批注**：选中文字 → 写下评论。
- 高亮**不破坏原网页**。
- **批注侧边栏**集中展示所有批注。
- AI 改完出新版本后，批注**自动跟到新位置**；原文被删则归档保留。
- **一键回灌**：把所有批注打包成给 AI 的指令并复制到剪贴板。
